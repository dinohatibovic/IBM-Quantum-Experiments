name: Release Checksum Verification

on:
  release:
    types: [published]

jobs:
  verify-checksums:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify tracked release files match data/checksums/release_sha256.txt
        run: bash scripts/verify_checksums.sh
